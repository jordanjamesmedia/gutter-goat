{
  "build": {
    "command": "npm install && npm run build",
    "output_dir": "dist",
    "environment": {
      "NODE_VERSION": "16.x"
    }
  },
  "routes": [
    { "src": "/images/*", "dest": "/images/$1" },
    { "src": "/*", "dest": "/$1" }
  ]
} 